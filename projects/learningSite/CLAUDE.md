@AGENTS.md

# Commit conventions

Every git commit message must be a single line only — no body, no bullet points, no description paragraphs. Do not include any signature or trailer (no `Co-Authored-By`, no `Claude-Session`, no generated-with links). This applies to every commit, always.
